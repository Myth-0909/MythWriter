# Hosted API used by production desktop packages (the /api suffix is optional).
VITE_API_BASE_URL=https://api.example.com

# Local-only packages may opt in explicitly. Do not enable for distributed builds.
# VITE_DESKTOP_ALLOW_LOCAL_API=true
